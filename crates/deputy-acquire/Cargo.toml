[package]
name = "deputy-acquire"
description = "Recursive dependency discovery and verified download into the dirty store."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords = ["supply-chain", "dependencies", "cargo", "vendoring", "download"]
categories = ["development-tools::cargo-plugins"]

[dependencies]
deputy-core = { path = "../deputy-core", version = "0.4.2" }
deputy-store = { path = "../deputy-store", version = "0.4.2" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
sha2 = "0.10"
