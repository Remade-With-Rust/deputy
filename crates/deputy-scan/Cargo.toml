[package]
name = "deputy-scan"
description = "Scanners: integrity, advisories, and dirty-vs-prod diffing."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords = ["security", "vulnerability", "advisory", "supply-chain", "scanner"]
categories = ["development-tools"]

[dependencies]
deputy-core = { path = "../deputy-core", version = "0.3.0" }
deputy-store = { path = "../deputy-store", version = "0.3.0" }
deputy-analyze = { path = "../deputy-analyze", version = "0.3.0" }
semver = "1"
serde = { workspace = true }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
